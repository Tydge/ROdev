# 变更记录

## 2026-09-02

- 完成 `world_ai` Step 4A 职业战斗策略接入：根据当前职业族、运行时已学技能和最终执行目标，临时把目标加入 OpenKore 原生 `attackSkillSlot` 的 `monsters` 条件。
- 新增 `CombatPolicy` 纯决策层和 `CombatRuntimeOverride` 可恢复内存覆盖层；`exec stop`、失败路径和插件卸载都恢复原技能过滤器，不写回 `config.txt`。
- 新增 `worldai combat inspect`，`exec status` 同步显示职业族、目标、技能、匹配槽和普攻 fallback。
- bot02 新增保守 Bash 原生技能槽；bot02–05 基线主动技能槽设 `maxAttempts 4`，防止吟唱持续被打断时无限重试。
- bot05 实机确认 Holy Light 对新动态目标 Baby Desert Wolf 生效并击杀；实测 `exec stop` 和 `plugin unload` 都能精确恢复，`maxAttempts` 到限后由 OpenKore 原生回退普攻。
- 全部 8 个离线测试文件共 133 项通过。bot03 仍为 Novice，bot02 / bot04 分别未学 Bash / Double Strafe，未强制改加点，其实战验收待角色自然满足前提。

## 2026-08-31

- 完成正式 `world_ai` Step 2 只读动态练级推荐器：角色实时快照、怪物×地图候选、安全过滤、可解释评分、地图共存怪风险、Top/inspect/reload 命令。
- `world_ai` 采用纯命令触发模型，无 `AI_pre` 高频钩子、后台轮询或自动重算；1,920 个候选离线全量评分约 22 ms，bot01 实际 Top 10 约 29～32 ms。
- 补充 Boss 刷新点硬过滤、重名怪物消歧、未知路线标记和原子 reload；`skill_range` 因缺乏有效区分度暂不参与风险。
- 启动脚本改为部署完整 `world_ai` 目录，并支持 `start-one bot01 manual`；正式插件只在 bot01 启用。
- 新增 OpenKore `world_ai_test` 最小实验插件，提供 `nav`、`hunt`、`status`、`stop` 命令。
- 验证 OpenKore 原生 `Task::MapRoute` 的单跳与多地图连续寻路，不包含自研路径算法或硬编码 Portal 坐标。
- 验证“Rocker → 按刷新量选择 `prt_fild07` → 自动前往 → 原战斗 AI 攻击 → 确认击杀”完整链路。
- 实验期间用正式 `configModify` 临时切换 `lockMap`，怪物白名单仅做内存覆盖；停止后均恢复原状态。
- 为项目建立 Git 基线，插件源码改由管理目录保存并由启动脚本部署到 OpenKore 运行目录。
- 移除早期测试给 `prt_fild08` 补怪的 `npc/custom/bot_training_spawns.txt` 及其 `npc/scripts_custom.conf` 引用；约定此后不直接修改地图刷怪。
- 新增 `world_ai` 离线索引生成器与 `map_index.json`（怪物↔地图，基于 pre-re mob_db 与静态刷怪脚本）。
- 将 rAthena 服务端自定义改动（ASPD 曲线、30× 卡片掉落、职业路线脚本）提交到 rAthena 仓库，使服务器状态可复现；索引元数据记录来源 commit、dirty 状态与输入哈希。

## 2026-08-27

- 将普通、Boss 和 MVP 卡片掉落倍率由基础 10× 提高为基础 30×；其他掉落与相对 LUK 加成保持不变。
- 将 Pre-Renewal 玩家基础攻速属性曲线改为 `c = 0.8`；保留 `AGI : DEX = 4 : 1`、有效属性 200 时的原版终点、攻速药/技能/装备修正和 190 ASPD 上限。

## 2026-08-25

- 将 Pre-Renewal DEX 咏唱从线性零咏唱改为 `T = T0 × 50 / (50 + DEX)` 的递减收益曲线。
- 将 Fire Ball 调整为 Lv1 100% MATK、每级 +20%、Lv10 280% MATK，保留范围边缘 75% 衰减。
- 启用 Prontera `(150,193)` 的 Reset Girl：技能/属性/两者重置费用分别为 5,000/5,000/9,000 Zeny。
- 普通材料、消耗品、装备与 Boss/MVP 非卡片掉落改为 2×，所有卡片改为 10×。
- 启用相对 LUK 掉落加成 `drops_by_luk2: 100`；100 LUK 理论上使当前掉率再翻倍。
- 重新编译并重启地图服，确认正常连接角色服并监听 5121。

## 2026-08-23

- 完成 M1 Mac + Parallels Windows 11 + rAthena Pre-Renewal 的本地可玩基线。
- 稳定登录、角色、地图、装备和存档。
- 修复 ItemInfo、MapInfo、宠物进化、冠军怪物资源等启动弹窗。
- 装备名称和地图名称改为英文。
- 正常任务列表改为英文。
- 禁用缺少 `reco_*.bmp` 的推荐任务宣传卡片，消除资源载入弹窗。
- 覆盖两套导航文件；小地图标记悬停乱码仍待修。
- 将 Base EXP、Job EXP、任务 EXP 从 1× 改为 2×。
- 新建 `/Users/wangtaizhi/娱乐/RO本地服` 管理目录和一键启停脚本。

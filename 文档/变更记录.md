# 变更记录

## 2026-08-31

- 新增 OpenKore `world_ai_test` 最小实验插件，提供 `nav`、`hunt`、`status`、`stop` 命令。
- 验证 OpenKore 原生 `Task::MapRoute` 的单跳与多地图连续寻路，不包含自研路径算法或硬编码 Portal 坐标。
- 验证“Rocker → 按刷新量选择 `prt_fild07` → 自动前往 → 原战斗 AI 攻击 → 确认击杀”完整链路。
- 实验期间用正式 `configModify` 临时切换 `lockMap`，怪物白名单仅做内存覆盖；停止后均恢复原状态。
- 为项目建立 Git 基线，插件源码改由管理目录保存并由启动脚本部署到 OpenKore 运行目录。

## 2026-08-27

- 将普通、Boss 和 MVP 卡片掉落倍率由基础 10× 提高为基础 30×；其他掉落与相对 LUK 加成保持不变。
- 将 Pre-Renewal 玩家基础攻速属性曲线改为 `c = 0.8`；保留 `AGI : DEX = 4 : 1`、有效属性 200 时的原版终点、攻速药/技能/装备修正和 190 ASPD 上限。

## 2026-08-25

- 将 Pre-Renewal DEX 咏唱从线性零咏唱改为 `T = T0 × 50 / (50 + DEX)` 的递减收益曲线。
- 将 Fire Ball 调整为 Lv1 100% MATK、每级 +20%、Lv10 280% MATK，保留范围边缘 75% 衰减。
- 启用 Prontera `(150,193)` 的 Reset Girl：技能/属性/两者重置费用分别为 5,000/5,000/9,000 Zeny。
- 普通材料、消耗品、装备与 Boss/MVP 非卡片掉落改为 2×，所有卡片改为 10×。
- 启用相对 LUK 掉落加成 `drops_by_luk2: 100`；100 LUK 理论上使当前掉率再翻倍。
- 重新编译并重启地图服，确认正常连接角色服并监听 5121。

## 2026-08-23

- 完成 M1 Mac + Parallels Windows 11 + rAthena Pre-Renewal 的本地可玩基线。
- 稳定登录、角色、地图、装备和存档。
- 修复 ItemInfo、MapInfo、宠物进化、冠军怪物资源等启动弹窗。
- 装备名称和地图名称改为英文。
- 正常任务列表改为英文。
- 禁用缺少 `reco_*.bmp` 的推荐任务宣传卡片，消除资源载入弹窗。
- 覆盖两套导航文件；小地图标记悬停乱码仍待修。
- 将 Base EXP、Job EXP、任务 EXP 从 1× 改为 2×。
- 新建 `/Users/wangtaizhi/娱乐/RO本地服` 管理目录和一键启停脚本。
